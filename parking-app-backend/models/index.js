const User = require('./User');
const ParkingSlot = require('./ParkingSlot');
const Reservation = require('./Reservation');

// Export models so they can be imported elsewhere
module.exports = {
  User,
  ParkingSlot,
  Reservation,
};